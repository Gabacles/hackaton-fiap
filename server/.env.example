# Example environment configuration for the EduShare backend.
# Copy this file to `.env` and set the appropriate values for your local
# environment. The DATABASE_URL should point to your PostgreSQL instance.

# The port the Express server will run on
PORT=4000

# PostgreSQL connection string. For example:
# DATABASE_URL=postgresql://user:password@localhost:5432/edushare
DATABASE_URL=

# Secret key used to sign JWTs. Make it long and random in production.
JWT_SECRET=mysecretkey